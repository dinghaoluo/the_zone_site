---
---

<div class="hero-backdrop" aria-hidden="true">
  <div class="hero-grid"></div>
</div>

<style>
  .hero-backdrop {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    z-index: 0;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.018) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.018) 1px, transparent 1px);
    background-size: 3.5rem 3.5rem;
    mask-image:
      radial-gradient(ellipse 70% 60% at 50% 40%, rgba(0, 0, 0, 0.45), transparent 80%);
    -webkit-mask-image:
      radial-gradient(ellipse 70% 60% at 50% 40%, rgba(0, 0, 0, 0.45), transparent 80%);
  }
</style>
